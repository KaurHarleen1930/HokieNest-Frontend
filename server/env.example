# 🔐 BACKEND ENVIRONMENT VARIABLES
# ⚠️  NEVER commit these to git - they contain sensitive information!

# Supabase Configuration (Private keys - server only)
SUPABASE_URL=your_supabase_project_url_here
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key_here

# JWT Secret for token signing (generate a strong random string)
JWT_SECRET=your_very_long_random_jwt_secret_key_here_at_least_32_characters

# Session Secret for express-session (generate a strong random string)
SESSION_SECRET=your_session_secret_key_here_at_least_32_characters

# 🔑 API Keys (Private - server only)
# RentCast API (Optional - for rental data)
RENTCAST_API_KEY=your_rentcast_api_key_here

# Email Service (Optional - for email verification)
RESEND_API_KEY=your_resend_api_key_here

# LLM Provider Selection: 'openai' or 'qwen'
LLM_PROVIDER=openai

# OpenAI API (Required if LLM_PROVIDER=openai)
OPENAI_API_KEY=your_openai_api_key_here
# Base URL: Leave empty or unset to use OpenAI official API (https://api.openai.com/v1)
# Or set to a custom URL for third-party OpenAI-compatible APIs
# OPENAI_BASE_URL=https://api.openai.com/v1
# Model selection: gpt-3.5-turbo (faster, cheaper) or gpt-4 (better quality, slower, more expensive)
# For official OpenAI API, you can also use: gpt-4, gpt-4-turbo-preview, gpt-4o, gpt-4o-mini, etc.
OPENAI_MODEL=gpt-3.5-turbo

# Qwen (DashScope) API (Required if LLM_PROVIDER=qwen)
# You can use OPENAI_API_KEY or DASHSCOPE_API_KEY
DASHSCOPE_API_KEY=your_dashscope_api_key_here
# Qwen model selection: qwen-max, qwen-plus, qwen-turbo, etc.
QWEN_MODEL=qwen-max

# Google OAuth (Optional - for Google login)
GOOGLE_CLIENT_ID=your_google_client_id_here
GOOGLE_CLIENT_SECRET=your_google_client_secret_here

# Database Configuration
DATABASE_URL=your_database_connection_string_here

# Server Configuration
PORT=4000
NODE_ENV=development

# Debug mode for verbose logging: 'true' or 'false'
# When enabled, shows detailed database queries, context content, and API requests
DEBUG=false

# CORS Origins (comma-separated)
CORS_ORIGINS=http://localhost:8080,http://localhost:5173,http://localhost:3000
